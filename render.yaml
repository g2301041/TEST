services:
  - type: web
    name: bear-weather-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server.py:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0